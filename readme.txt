Our program reads in a file called example.txt and reads for curly braces using regex to find words to replace.
It then takes input from the user and replaces the word surrounded by curly braces and writes the newline to an out.txt file.
This input can only be a single continuious word.
